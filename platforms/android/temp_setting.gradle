android {
            defaultConfig {
                applicationId = "org.nativescript.skladscaner"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }